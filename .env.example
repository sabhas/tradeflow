# Copy to .env and configure
# API
PORT=3001
# Database (used by api, db migrate, db seed)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/tradeflow
# Auth
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d
